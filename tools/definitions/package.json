{
  "name": "touchstone-definitions-checks",
  "private": true,
  "description": "Checks and generators for definitions/, the YAML-LD test definitions. Run: python check.py",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "jsonld": "9.0.0",
    "yaml": "2.9.1"
  }
}
